// Casca de rota. A implementação vive no módulo `store`; o Next.js exige que o
// ficheiro de rota esteja sob `app/`, por isso a fronteira do módulo é o import.
export { default } from "@/modules/store/components/OrdersPage";
